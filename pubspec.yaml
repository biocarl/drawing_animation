name: drawing_animation
description: An dart-only library for gradually painting SVG path objects on canvas (drawing line animation).
version: 0.0.2
author: Carl Hauck <cahaucks@gmail.com>
repository: https://github.com/biocarl/drawing_animation
homepage: https://github.com/biocarl/drawing_animation

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  xml: ^3.2.5
  path_parsing: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.5.1
  mockito: ^4.0.0

# The following section is specific to Flutter.
flutter:
